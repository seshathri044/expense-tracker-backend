# =========================
# Application Name
# =========================
spring.application.name=ExpenseTracker

# =========================
# Database Configuration
# =========================
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/expense_tracker?useSSL=false&serverTimezone=Asia/Kolkata&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=SESHATHRIm#21

# =========================
# JPA/Hibernate Configuration
# =========================
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# =========================
# JWT Configuration
# =========================
jwt.secret.key=thisisthesecretkeyievercreatedinmydevelopmentcareer

# =========================
# Server Configuration
# =========================
# IMPORTANT: Context path means all endpoints will be prefixed with this
# Remove or comment out if you don't want the /api/v1.0 prefix
# server.servlet.context-path=/api/v1.0

# Alternative: Use a simpler context path
server.servlet.context-path=/api

# =========================
# Email Configuration (Brevo SMTP)
# =========================
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=97fcc4001@smtp-brevo.com
spring.mail.password=ZtG8dXFQBYxLI4Uh

# SMTP Properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp-relay.brevo.com
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2

# Connection settings
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# From email address (must be verified in Brevo account)
spring.mail.properties.mail.smtp.from=mseshathri507@gmail.com

# Default encoding
spring.mail.default-encoding=UTF-8

# =========================
# Logging Configuration
# =========================
logging.level.com.example.ExpenseTracker=INFO
logging.level.org.springframework.mail=INFO
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# =========================
# Swagger Configuration (Optional)
# =========================
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true